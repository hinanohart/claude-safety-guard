{
  "_comment": "Drop this into ~/.claude/settings.json or <project>/.claude/settings.json. The PreToolUse hook routes every Bash command through claude-safety-guard before Claude Code executes it.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "claude-safety-guard hook"
          }
        ]
      }
    ]
  }
}
